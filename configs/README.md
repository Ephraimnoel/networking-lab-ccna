# Configurations

**Status: Planned — no completed configurations are included yet**

This directory is reserved for sanitized configuration output captured after I personally perform the labs. It intentionally does not contain fabricated router or switch configurations.

## What to add later

- `router-config.txt`
- `switch-1-config.txt`
- `switch-2-config.txt`
- Any other configuration files needed to explain the final design

For each file, record:

- Device role and model
- Date captured
- Lab stage
- Whether the configuration is baseline or post-troubleshooting
- Any commands or values redacted for safety

## Safety and authenticity

Only add configurations generated by my own Packet Tracer work. Remove passwords, secrets, tokens, personal information, and anything that should not be public. A configuration template is not proof that a device was configured.
